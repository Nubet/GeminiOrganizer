{
  "name": "geminiorganizer",
  "private": true,
  "version": "1.1",
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "dev": "extension dev",
    "build": "extension build",
    "build:chrome": "extension build --browser chrome",
    "build:firefox": "extension build --browser firefox",
    "build:all": "bun run build:chrome && bun run build:firefox",
    "zip:chrome": "extension build --browser chrome --zip",
    "zip:firefox": "extension build --browser firefox --zip"
  },
  "devDependencies": {
    "extension": "4.1.18",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "dexie": "^4.0.11",
    "preact": "^10.26.4"
  }
}
